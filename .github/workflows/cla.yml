name: "CLA Assistant"
on:
  issue_comment:
    types: [created]
  pull_request_target:
    types: [opened,synchronize]
  merge_group:

permissions:
  actions: write
  contents: write # to store signatures on cla-signatures branch
  pull-requests: write
  statuses: write

jobs:
  CLAssistant:
    runs-on: ubuntu-latest
    # pull[bot] syncs upstream `main` into this fork with locked pull requests, and no
    # cla-signatures branch exists here, so the action can neither comment nor record a
    # signature. Contributor CLAs are enforced upstream, where the changes are authored.
    if: github.actor != 'pull[bot]'
    steps:
      - name: "CLA Assistant"
        if: (github.event.issue.pull_request && (github.event.comment.body == 'recheck' || github.event.comment.body == 'I have read the CLA Document and I hereby sign the CLA')) || github.event_name == 'pull_request_target'
        uses: contributor-assistant/github-action@v2.6.1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          path-to-signatures: 'signatures/version1/cla.json'
          path-to-document: 'https://www.cloudflare.com/cla/'
          # branch should not be protected
          branch: 'cla-signatures'
          allowlist: dependabot[bot],workers-devprod,github-actions[bot]
          lock-pullrequest-aftermerge: false
